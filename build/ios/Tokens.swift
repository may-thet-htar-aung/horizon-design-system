
//
// Tokens.swift
//

// Do not edit directly, this file was auto-generated.


import UIKit

public class Tokens {
    public static let bodyLg = [object Object] /** horizon.type.body.lg — web and mobile. */
    public static let bodyLgBold = [object Object] /** horizon.type.body.lg.bold — Inter Semi Bold 600. */
    public static let bodyMd = [object Object] /** horizon.type.body.md — web and mobile. */
    public static let bodySm = [object Object] /** horizon.type.body.sm — web and mobile. */
    public static let borderRadius1 = CGFloat(32.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadius2 = CGFloat(64.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadius3 = CGFloat(96.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadius4 = CGFloat(128.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadius5 = CGFloat(192.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadius6 = CGFloat(256.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadius7 = CGFloat(15984.00) /** Corner radius step. 7 is the pill. */
    public static let borderRadiusControl = CGFloat(192.00) /** Button, input, chip. 12 web and mobile, 2 back office. */
    public static let borderRadiusFull = CGFloat(15984.00) /** Pill and avatar. */
    public static let borderRadiusSurface = CGFloat(256.00) /** Card, panel, modal. */
    public static let borderSize1 = CGFloat(16.00) /** Stroke weight step. */
    public static let borderSize2 = CGFloat(32.00) /** Stroke weight step. */
    public static let borderSize3 = CGFloat(64.00) /** Stroke weight step. */
    public static let borderWidthBold = CGFloat(32.00) /** Emphasised outline. */
    public static let borderWidthDefault = CGFloat(16.00) /** Card, input, divider. */
    public static let borderWidthFocused = CGFloat(32.00) /** Focus ring thickness. */
    public static let colorAmber50 = UIColor(red: 1.000, green: 0.957, blue: 0.878, alpha: 1) /** Subtle warning surface. */
    public static let colorAmber700 = UIColor(red: 0.541, green: 0.353, blue: 0.000, alpha: 1) /** Warning. 108 layers. */
    public static let colorBgBase = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** Page ground. */
    public static let colorBgBaseInverse = UIColor(red: 0.059, green: 0.184, blue: 0.420, alpha: 1) /** Sidebar and dark panels. */
    public static let colorBgBaseSubtle = UIColor(red: 0.969, green: 0.976, blue: 0.984, alpha: 1) /** Sunken ground. Footer, table stripe. */
    public static let colorBgInfoSubtle = UIColor(red: 0.933, green: 0.957, blue: 1.000, alpha: 1) /** Info banner and tag ground. */
    public static let colorBgNegative = UIColor(red: 0.647, green: 0.149, blue: 0.114, alpha: 1) /** Destructive action fill. */
    public static let colorBgNegativeSubtle = UIColor(red: 0.992, green: 0.925, blue: 0.918, alpha: 1) /** Error banner ground. */
    public static let colorBgOverlay = UIColor(red: 0.106, green: 0.153, blue: 0.200, alpha: 0.550000011920929) /** Scrim behind modals and drawers. */
    public static let colorBgPositiveSubtle = UIColor(red: 0.890, green: 0.961, blue: 0.918, alpha: 1) /** Success banner ground. */
    public static let colorBgPrimary = UIColor(red: 0.114, green: 0.306, blue: 0.847, alpha: 1) /** Primary action fill. */
    public static let colorBgPrimaryDisabled = UIColor(red: 0.843, green: 0.871, blue: 0.906, alpha: 1) /** Primary action, unavailable. */
    public static let colorBgPrimaryHovered = UIColor(red: 0.094, green: 0.251, blue: 0.694, alpha: 1) /** Primary action, pointer over. */
    public static let colorBgPrimaryPressed = UIColor(red: 0.075, green: 0.200, blue: 0.561, alpha: 1) /** Primary action, held down. */
    public static let colorBgPrimarySubtle = UIColor(red: 0.933, green: 0.957, blue: 1.000, alpha: 1) /** Quiet primary ground. Selected chip. */
    public static let colorBgSecondary = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** Outlined action fill. */
    public static let colorBgSecondaryHovered = UIColor(red: 0.945, green: 0.957, blue: 0.969, alpha: 1) /** Outlined action, pointer over. */
    public static let colorBgSurfacePrimary = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** Card and panel surface. */
    public static let colorBgSurfaceSecondary = UIColor(red: 0.969, green: 0.976, blue: 0.984, alpha: 1) /** Nested or sunken surface. */
    public static let colorBgWarningSubtle = UIColor(red: 1.000, green: 0.957, blue: 0.878, alpha: 1) /** Warning banner ground. */
    public static let colorBlue100 = UIColor(red: 0.788, green: 0.851, blue: 0.973, alpha: 1) /** Production tint. */
    public static let colorBlue200 = UIColor(red: 0.498, green: 0.659, blue: 0.933, alpha: 1) /** Production tint. */
    public static let colorBlue300 = UIColor(red: 0.373, green: 0.639, blue: 0.859, alpha: 1) /** Light blue. */
    public static let colorBlue400 = UIColor(red: 0.231, green: 0.510, blue: 0.965, alpha: 1) /** Button2, SignInBtn. Action colour on dark. */
    public static let colorBlue50 = UIColor(red: 0.933, green: 0.957, blue: 1.000, alpha: 1) /** Tag and icon tile. */
    public static let colorBlue500 = UIColor(red: 0.180, green: 0.486, blue: 0.769, alpha: 1) /** Orphan Button and the wordmark. */
    public static let colorBlue600 = UIColor(red: 0.114, green: 0.306, blue: 0.847, alpha: 1) /** btn primary, btn-cta, book now button, btn admin. */
    public static let colorBlue700 = UIColor(red: 0.094, green: 0.251, blue: 0.694, alpha: 1) /** DERIVED from 600 at 82% lightness. Hover. */
    public static let colorBlue800 = UIColor(red: 0.075, green: 0.200, blue: 0.561, alpha: 1) /** DERIVED from 600 at 66% lightness. Pressed. */
    public static let colorBorderBold = UIColor(red: 0.843, green: 0.871, blue: 0.906, alpha: 1) /** Outlined control at rest. */
    public static let colorBorderDefault = UIColor(red: 0.918, green: 0.937, blue: 0.957, alpha: 1) /** Card, input, divider. */
    public static let colorBorderDisabled = UIColor(red: 0.918, green: 0.937, blue: 0.957, alpha: 1) /** Border of a disabled control. */
    public static let colorBorderFocused = UIColor(red: 0.114, green: 0.306, blue: 0.847, alpha: 1) /** Keyboard focus ring. Follows the primary action. */
    public static let colorBorderInverse = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** Border on an inverse surface. */
    public static let colorBorderNegative = UIColor(red: 0.878, green: 0.169, blue: 0.169, alpha: 1) /** Field in error. */
    public static let colorBorderSubtle = UIColor(red: 0.945, green: 0.957, blue: 0.969, alpha: 1) /** Faint divider. */
    public static let colorGreen50 = UIColor(red: 0.890, green: 0.961, blue: 0.918, alpha: 1) /** Subtle positive surface. */
    public static let colorGreen600 = UIColor(red: 0.122, green: 0.541, blue: 0.298, alpha: 1) /** Positive. 203 layers, all three platforms. */
    public static let colorGreen800 = UIColor(red: 0.078, green: 0.325, blue: 0.176, alpha: 1) /** Positive on a subtle surface. */
    public static let colorIconAccent = UIColor(red: 0.941, green: 0.576, blue: 0.169, alpha: 1) /** Rating star. */
    public static let colorIconInverse = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** Icon on a filled action. */
    public static let colorIconNegative = UIColor(red: 0.647, green: 0.149, blue: 0.114, alpha: 1) /** Failed, cancelled. */
    public static let colorIconPositive = UIColor(red: 0.122, green: 0.541, blue: 0.298, alpha: 1) /** Confirmed. */
    public static let colorIconPrimary = UIColor(red: 0.106, green: 0.153, blue: 0.200, alpha: 1) /** Default icon. */
    public static let colorIconSubtle = UIColor(red: 0.541, green: 0.580, blue: 0.651, alpha: 1) /** Low-emphasis icon. */
    public static let colorIconWarning = UIColor(red: 0.541, green: 0.353, blue: 0.000, alpha: 1) /** Pending, expiring. */
    public static let colorNavy700 = UIColor(red: 0.122, green: 0.361, blue: 0.561, alpha: 1) /** Back-office accent, visited link. */
    public static let colorNavy800 = UIColor(red: 0.059, green: 0.184, blue: 0.420, alpha: 1) /** Sidebar v2, PrimaryButton2. */
    public static let colorNavy900 = UIColor(red: 0.039, green: 0.141, blue: 0.314, alpha: 1) /** Sidebar active item. */
    public static let colorNeutral0 = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** White. */
    public static let colorNeutral100 = UIColor(red: 0.945, green: 0.957, blue: 0.969, alpha: 1) /** Sunken surface. */
    public static let colorNeutral200 = UIColor(red: 0.918, green: 0.937, blue: 0.957, alpha: 1) /** The border grey. 8 components use it. */
    public static let colorNeutral300 = UIColor(red: 0.843, green: 0.871, blue: 0.906, alpha: 1) /** Strong border. */
    public static let colorNeutral400 = UIColor(red: 0.725, green: 0.780, blue: 0.839, alpha: 1) /** Disabled. */
    public static let colorNeutral50 = UIColor(red: 0.969, green: 0.976, blue: 0.984, alpha: 1) /** Sunken ground. */
    public static let colorNeutral500 = UIColor(red: 0.541, green: 0.580, blue: 0.651, alpha: 1) /** Subtle text. 1,867 layers in production. */
    public static let colorNeutral600 = UIColor(red: 0.478, green: 0.533, blue: 0.600, alpha: 1) /** Between subtle and secondary. */
    public static let colorNeutral700 = UIColor(red: 0.353, green: 0.420, blue: 0.494, alpha: 1) /** Secondary text. 2,640 layers in production. */
    public static let colorNeutral800 = UIColor(red: 0.227, green: 0.271, blue: 0.325, alpha: 1) /** Strong secondary. */
    public static let colorNeutral900 = UIColor(red: 0.106, green: 0.153, blue: 0.200, alpha: 1) /** Primary text. 2,382 layers in production. */
    public static let colorNeutral900a = UIColor(red: 0.106, green: 0.153, blue: 0.200, alpha: 0.550000011920929) /** Scrim behind modals and drawers. 55% alpha. */
    public static let colorNeutral950 = UIColor(red: 0.071, green: 0.094, blue: 0.122, alpha: 1) /** Max contrast. Dark-mode ground. */
    public static let colorOrange500 = UIColor(red: 0.941, green: 0.576, blue: 0.169, alpha: 1) /** Accent. Review rating. */
    public static let colorRed50 = UIColor(red: 0.992, green: 0.925, blue: 0.918, alpha: 1) /** Subtle negative surface. */
    public static let colorRed500 = UIColor(red: 0.878, green: 0.169, blue: 0.169, alpha: 1) /** Field error border. */
    public static let colorRed600 = UIColor(red: 0.757, green: 0.212, blue: 0.184, alpha: 1) /** Negative icon. 84 layers. */
    public static let colorRed700 = UIColor(red: 0.647, green: 0.149, blue: 0.114, alpha: 1) /** Negative text. 67 layers. */
    public static let colorTextAccent = UIColor(red: 0.941, green: 0.576, blue: 0.169, alpha: 1) /** Rating and highlight. */
    public static let colorTextDisabled = UIColor(red: 0.725, green: 0.780, blue: 0.839, alpha: 1) /** Text inside a disabled control. */
    public static let colorTextInverse = UIColor(red: 1.000, green: 1.000, blue: 1.000, alpha: 1) /** Text on a filled action or inverse surface. */
    public static let colorTextLink = UIColor(red: 0.114, green: 0.306, blue: 0.847, alpha: 1) /** Inline link. Follows the primary action rather than restating blue. */
    public static let colorTextLinkHovered = UIColor(red: 0.094, green: 0.251, blue: 0.694, alpha: 1) /** Link, pointer over. */
    public static let colorTextLinkVisited = UIColor(red: 0.122, green: 0.361, blue: 0.561, alpha: 1) /** Link already followed. */
    public static let colorTextNegative = UIColor(red: 0.647, green: 0.149, blue: 0.114, alpha: 1) /** Error copy. */
    public static let colorTextPositive = UIColor(red: 0.122, green: 0.541, blue: 0.298, alpha: 1) /** Confirmation copy. */
    public static let colorTextPrimary = UIColor(red: 0.106, green: 0.153, blue: 0.200, alpha: 1) /** Body copy and titles. */
    public static let colorTextSecondary = UIColor(red: 0.353, green: 0.420, blue: 0.494, alpha: 1) /** Supporting copy, table cells. */
    public static let colorTextSubtle = UIColor(red: 0.541, green: 0.580, blue: 0.651, alpha: 1) /** Placeholder, metadata, captions. */
    public static let colorTextSubtlest = UIColor(red: 0.725, green: 0.780, blue: 0.839, alpha: 1) /** Lowest-emphasis text. */
    public static let colorTextWarning = UIColor(red: 0.541, green: 0.353, blue: 0.000, alpha: 1) /** Caution copy. */
    public static let displayLg = [object Object] /** horizon.type.display.lg — web and mobile. */
    public static let displayMd = [object Object] /** horizon.type.display.md — web and mobile. */
    public static let displaySm = [object Object] /** horizon.type.display.sm — web and mobile. */
    public static let elevationLevel1 = [object Object],[object Object] /** Card and raised surface. Use where a card sits on a busy ground; keep border-only on quiet grounds. New to the system — the audited products use zero shadows today. */
    public static let elevationLevel2 = [object Object],[object Object] /** Sticky header, raised panel. New to the system — the audited products use zero shadows today. */
    public static let elevationLevel3 = [object Object],[object Object] /** Dropdown, popover, tooltip. New to the system — the audited products use zero shadows today. */
    public static let elevationLevel4 = [object Object],[object Object] /** Modal and dialog. New to the system — the audited products use zero shadows today. */
    public static let elevationLevel5 = [object Object],[object Object] /** Full-screen overlay, command palette. New to the system — the audited products use zero shadows today. */
    public static let familyDefault = Inter /** Inter. Already 100% consistent across all three platforms — the one thing that was. */
    public static let headlineLg = [object Object] /** horizon.type.headline.lg — web and mobile. */
    public static let headlineLgBold = [object Object] /** horizon.type.headline.lg.bold — Inter Semi Bold 600. */
    public static let headlineMd = [object Object] /** horizon.type.headline.md — web and mobile. */
    public static let headlineSm = [object Object] /** horizon.type.headline.sm — web and mobile. */
    public static let labelLg = [object Object] /** horizon.type.label.lg — web and mobile. */
    public static let labelLgBold = [object Object] /** horizon.type.label.lg.bold — Inter Semi Bold 600. */
    public static let labelMd = [object Object] /** horizon.type.label.md — web and mobile. */
    public static let labelSm = [object Object] /** horizon.type.label.sm — web and mobile. */
    public static let lineheightBodyLg = CGFloat(384.00) /** Material 3 body large lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightBodyMd = CGFloat(320.00) /** Material 3 body medium lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightBodySm = CGFloat(256.00) /** Material 3 body small lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightDisplayLg = CGFloat(1024.00) /** Material 3 display large lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightDisplayMd = CGFloat(832.00) /** Material 3 display medium lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightDisplaySm = CGFloat(704.00) /** Material 3 display small lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightHeadlineLg = CGFloat(640.00) /** Material 3 headline large lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightHeadlineMd = CGFloat(576.00) /** Material 3 headline medium lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightHeadlineSm = CGFloat(512.00) /** Material 3 headline small lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightLabelLg = CGFloat(320.00) /** Material 3 label large lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightLabelMd = CGFloat(256.00) /** Material 3 label medium lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightLabelSm = CGFloat(256.00) /** Material 3 label small lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightTitleLg = CGFloat(448.00) /** Material 3 title large lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightTitleMd = CGFloat(384.00) /** Material 3 title medium lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let lineheightTitleSm = CGFloat(320.00) /** Material 3 title small lineHeight. Web and mobile share the comfortable value; back office is compact. */
    public static let size1 = CGFloat(256.00) /** Control and icon dimension step. */
    public static let size10 = CGFloat(768.00) /** Control and icon dimension step. */
    public static let size2 = CGFloat(320.00) /** Control and icon dimension step. */
    public static let size3 = CGFloat(384.00) /** Control and icon dimension step. */
    public static let size4 = CGFloat(448.00) /** Control and icon dimension step. */
    public static let size5 = CGFloat(512.00) /** Control and icon dimension step. */
    public static let size6 = CGFloat(544.00) /** Control and icon dimension step. */
    public static let size7 = CGFloat(576.00) /** Control and icon dimension step. */
    public static let size8 = CGFloat(640.00) /** Control and icon dimension step. */
    public static let size9 = CGFloat(704.00) /** Control and icon dimension step. */
    public static let sizeBodyLg = CGFloat(256.00) /** Material 3 body large size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeBodyMd = CGFloat(224.00) /** Material 3 body medium size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeBodySm = CGFloat(192.00) /** Material 3 body small size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeControlLg = CGFloat(768.00) /** Prominent action height. */
    public static let sizeControlMd = CGFloat(768.00) /** Default button and input height. 40 web, 48 mobile, 34 back office. */
    public static let sizeControlSm = CGFloat(512.00) /** Dense button and chip height. */
    public static let sizeDisplayLg = CGFloat(912.00) /** Material 3 display large size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeDisplayMd = CGFloat(720.00) /** Material 3 display medium size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeDisplaySm = CGFloat(576.00) /** Material 3 display small size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeHeadlineLg = CGFloat(512.00) /** Material 3 headline large size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeHeadlineMd = CGFloat(448.00) /** Material 3 headline medium size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeHeadlineSm = CGFloat(384.00) /** Material 3 headline small size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeIconLg = CGFloat(448.00) /** Prominent icon. */
    public static let sizeIconMd = CGFloat(384.00) /** Default icon. */
    public static let sizeIconSm = CGFloat(256.00) /** Inline icon. */
    public static let sizeLabelLg = CGFloat(224.00) /** Material 3 label large size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeLabelMd = CGFloat(192.00) /** Material 3 label medium size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeLabelSm = CGFloat(176.00) /** Material 3 label small size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeTitleLg = CGFloat(352.00) /** Material 3 title large size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeTitleMd = CGFloat(256.00) /** Material 3 title medium size. Web and mobile share the comfortable value; back office is compact. */
    public static let sizeTitleSm = CGFloat(224.00) /** Material 3 title small size. Web and mobile share the comfortable value; back office is compact. */
    public static let spacing1 = CGFloat(64.00) /** 4px base unit. */
    public static let spacing10 = CGFloat(1024.00) /** 4px base unit. */
    public static let spacing2 = CGFloat(128.00) /** 4px base unit. */
    public static let spacing3 = CGFloat(192.00) /** 4px base unit. */
    public static let spacing4 = CGFloat(256.00) /** 4px base unit. */
    public static let spacing5 = CGFloat(320.00) /** 4px base unit. */
    public static let spacing6 = CGFloat(384.00) /** 4px base unit. */
    public static let spacing7 = CGFloat(512.00) /** 4px base unit. */
    public static let spacing8 = CGFloat(640.00) /** 4px base unit. */
    public static let spacing9 = CGFloat(768.00) /** 4px base unit. */
    public static let spacingGap2xs = CGFloat(64.00) /** Icon to label. */
    public static let spacingGapLg = CGFloat(384.00) /** Between sections. */
    public static let spacingGapMd = CGFloat(256.00) /** Between blocks. */
    public static let spacingGapSm = CGFloat(192.00) /** Within a group. */
    public static let spacingGapXl = CGFloat(640.00) /** Between major regions. */
    public static let spacingGapXs = CGFloat(128.00) /** Between related lines. */
    public static let spacingMarginLg = CGFloat(512.00) /** Section outer offset. */
    public static let spacingMarginMd = CGFloat(256.00) /** Default outer offset. */
    public static let spacingMarginSm = CGFloat(192.00) /** Small outer offset. */
    public static let spacingMarginXl = CGFloat(768.00) /** Page-level outer offset. */
    public static let spacingPadding2xs = CGFloat(64.00) /** Tightest inset. */
    public static let spacingPaddingLg = CGFloat(384.00) /** Panel and section inset. */
    public static let spacingPaddingMd = CGFloat(320.00) /** Default control and card inset. */
    public static let spacingPaddingSm = CGFloat(192.00) /** Dense control inset. */
    public static let spacingPaddingXl = CGFloat(512.00) /** Page gutter. */
    public static let spacingPaddingXs = CGFloat(128.00) /** Chip and tag inset. */
    public static let titleLg = [object Object] /** horizon.type.title.lg — web and mobile. */
    public static let titleLgBold = [object Object] /** horizon.type.title.lg.bold — Inter Semi Bold 600. */
    public static let titleMd = [object Object] /** horizon.type.title.md — web and mobile. */
    public static let titleMdBold = [object Object] /** horizon.type.title.md.bold — Inter Semi Bold 600. */
    public static let titleSm = [object Object] /** horizon.type.title.sm — web and mobile. */
    public static let titleSmBold = [object Object] /** horizon.type.title.sm.bold — Inter Semi Bold 600. */
    public static let trackingBodyLg = CGFloat(8.00) /** Material 3 body large tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingBodyMd = CGFloat(4.00) /** Material 3 body medium tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingBodySm = CGFloat(6.40) /** Material 3 body small tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingDisplayLg = CGFloat(-4.00) /** Material 3 display large tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingDisplayMd = CGFloat(0.00) /** Material 3 display medium tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingDisplaySm = CGFloat(0.00) /** Material 3 display small tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingHeadlineLg = CGFloat(0.00) /** Material 3 headline large tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingHeadlineMd = CGFloat(0.00) /** Material 3 headline medium tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingHeadlineSm = CGFloat(0.00) /** Material 3 headline small tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingLabelLg = CGFloat(1.60) /** Material 3 label large tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingLabelMd = CGFloat(8.00) /** Material 3 label medium tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingLabelSm = CGFloat(8.00) /** Material 3 label small tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingTitleLg = CGFloat(0.00) /** Material 3 title large tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingTitleMd = CGFloat(2.40) /** Material 3 title medium tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let trackingTitleSm = CGFloat(1.60) /** Material 3 title small tracking. Web and mobile share the comfortable value; back office is compact. */
    public static let weightBold = 600 /** Emphasised titles, buttons and prices. Extension of the M3 base scale. */
    public static let weightMedium = 500 /** Titles and labels. Material 3 base emphasis. */
    public static let weightRegular = 400 /** Body and long-form copy. */
}